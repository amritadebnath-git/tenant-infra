

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./app:/app
    depends_on:
      - db

  db:
    image: busybox
    volumes:
      - ./app/users.db:/data/users.db
    command: tail -f /dev/null

